grumphp:
    git_dir: .
    bin_dir: vendor/bin
    ascii:
        failed: tests/_data/jirhub-ko.txt
        succeeded: tests/_data/jirhub-ok.txt
    tasks:
        phpcs:
              standard: [PSR2]
              whitelist_patterns: [src]
        codeception: ~
